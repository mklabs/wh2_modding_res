-- if the scripts are in the root folder (the same level as .vscode, ie. root/.vscode or root/script_name, use this
-- require("script_name")
-- if the scripts are in a subfolder, use this
-- require("path/to/the/script_name")

-- do NOT include the ".lua" extension as a parameter